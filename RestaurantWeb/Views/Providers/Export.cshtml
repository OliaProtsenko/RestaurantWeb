@model RestaurantWeb.Provider
@{
    ViewData["Title"] = "Export";
}
<div>
    <h4>Завантажити Excel-звіт</h4>

    <div class="row">
        <div class="col-md-4">
            <h5>Фільтри</h5>
            <form asp-action="Export">
                <div class="form-group">
                    <label asp-for="Id" class="control-label"></label>
                    <select asp-for="Id" class="form-control" asp-items="@ViewBag.ProviderLists">
                        <option selected>–Не вибрана–</option>
                    </select>





                    <div class="form-group">
                        <input type="submit" class="btn btn-primary" value="Завантажити" />
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>